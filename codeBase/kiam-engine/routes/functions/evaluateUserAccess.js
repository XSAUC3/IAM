
//#region FUNCTIONS FOR CREATING RESPONSE OBJECT AND EVALUATING USER ACCESS

module.exports.evaluateConstraintForPolicy = (policy,environmentAttributes) => {
    //RETURN TRUE or FALSE BASED on Constraint
    // CHINMAY
}

module.exports.evaluatePolicy = (environmentAttributes,resourceAction) => {
    // RETURN TRUE or FALSE
    // CHINMAY
}

module.exports.getPolicyType = (policy) => {
    // RETURN POLICY TYPE
    // Vrushank
}

module.exports.addToResponseObject = (resource, finalprivilege, environmentAttributes) => {
    // ADD 
    // Vrushank
}

//#endregion
