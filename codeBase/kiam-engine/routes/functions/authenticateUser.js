
// DHRUVESH

//#region FUNCTIONS FOR CHECKING USER AUTHENTICATION IN KIAM & LDAP

module.exports.userExistsInKIAM = (username) => {
     // RETURN TRUE OR FALSE
};

module.exports.AuthenticateUser = (username, password) => {
     // RETURN TRUE OR FALSE    
};

module.exports.fetchUserDetailsFromUserStore = (username, password) => {
     // RETURN AN ARRAY OF OBJECTS USER DETAILS
};

module.exports.fetchUserDetailsFromKIAM = (username) => {
    // RETURN AN ARRAY OF OBJECTS USER DETAILS
};

//#endregion
